# 포트폴리오/면접 정리 작업 계획 (세션 간 이어가기용)

이 파일은 여러 세션에 걸쳐 이 정리 작업을 계속하기 위한 진행 상황 기록이다.
새 세션에서 이 작업을 이어갈 때는 이 파일부터 읽을 것.

## 배경 / 목표
- 국비지원 게임클라이언트(Unity) 과정 수료. 실습 코드를 포트폴리오 + 기술면접 준비용으로 재정리.
- 모든 구현을 나열하지 않고 "핵심 개념 + 그 개념을 가장 잘 보여주는 대표 예제"로 압축.
- 결과물은 이 폴더(`C:\Study\ST_4_GAMEDEV`)에 언어/기술별 md 파일로 저장.

## 중요: 소스 코드 위치
`ST_4_GAMEDEV` 폴더 자체는 원래 비어있었음(정리 결과물 저장용). **실제 실습 코드는 아래 폴더들에 있음**:
- `C:\Study\C` — C/C++ 실습
- `C:\Study\MFC` — MFC 실습
- `C:\Study\D3D` — WinAPI/Direct3D 실습 (`D3D_ST`, `BOARD/Engine` 등)
- `C:\Study\Unity` — Unity 프로젝트 다수

**인코딩 주의**: 원본 `.c`/`.cpp`/`.h` 파일 다수가 CP949(한글 완성형)로 저장되어 있어 UTF-8 도구로 그냥 읽으면 주석이 깨진다. 읽기 전에 `iconv -f CP949 -t UTF-8`로 변환해서 볼 것. 주석에 실습 의도/의사코드가 자세히 적혀있는 경우가 많으므로 반드시 변환해서 확인해야 함 (사용자가 직접 강조한 사항).

## 정리 포맷 (모든 언어 공통, 개념 단위로 반복)
```
### [개념 이름]
- **한 줄 정의**
- **왜 중요한가**
- **내 코드에서 어떻게 썼는지**: 실제 경로 + 최소 스니펫 (전체 파일 X)
- **주의할 점 / 자주 나오는 꼬리 질문**
- **최신 동향 (웹서칭 결과)**: 지금도 유효한지, 대체 기술 있는지. 확인 안 되면 "확인 필요"라고 표시
```
- 각 파일 최상단에 개념 목차(TOC)를 먼저 넣는다.
- 코드가 실제로 어떤 언어인지(파일명과 무관하게) 확인 후 분류한다 — 예: `C:\Study\C\Wordle_c`는 이름과 달리 실제로는 C++(class, enum class, iostream) 코드였음.

## 진행 순서 (사용자가 지정한 순서, 한 번에 하나씩)
1. **C언어** → `c_핵심정리.md` — ✅ 완료 (2026-07-27)
2. **C++** → `cpp_핵심정리.md` — ✅ 완료 (2026-07-27, Part1 기초 + Part2 중급/고급(API/CPP_PLUS) 포함)
3. **WinAPI + MFC** → `winapi_핵심정리.md` / `mfc_핵심정리.md` (파일은 2개, 작업은 한 번에 진행) — ✅ 완료 (2026-07-27)
4. **Unity** → `unity_핵심정리.md` — ✅ 완료 (2026-07-29, 프로젝트 1~26번 전부 처리 — 상세 진행상황은 unity_핵심정리.md 상단 표 참고)
5. **5개 파일 관통 종합 정리** (별도 파일, 예: C 메모리 개념 → C++ 객체 → Unity GC 흐름) — ⏳ 대기 중, **사용자가 다른 모델로 진행할 예정** (이 세션에서 자동으로 진행하지 말 것)

## 3단계(C++) 추가 확정 사항 (2026-07-27)
- `C:\Study\API\CPP_PLUS`(싱글톤, 스택/이중연결리스트 템플릿화, TCP·UDP 소켓, C++11 동시성 심화)는 "같은 시기에 배운 C++ 중급/고급 과정"이라는 사용자 확인에 따라 WinAPI가 아니라 `cpp_핵심정리.md`의 Part 2로 포함시킴. `API/CPP_PLUS`의 `FMODENGIN` 헤더는 실제 사용처가 없어 제외.

## 4단계(WinAPI+MFC) 스코프 확정 사항 (2026-07-27)
- 소스 폴더: `C:\Study\API`(단, `CPP_PLUS` 제외) + `C:\Study\MFC`.
- **`C:\Study\D3D\BOARD\Engine`은 사용자가 개인적으로 별도 작업한 프로젝트라서 이번 정리에서 완전히 제외.** `D3D\D3D_ST`도 이번 WinAPI 단계에서는 다루지 않음(별도 그래픽스/D3D 정리가 필요하면 나중에 논의).
- **`C:\Study\lookis` 폴더는 다른 시설(교육기관)에서 배운 내용이라 전체 제외.**
- 파일 구조: `winapi_핵심정리.md`와 `mfc_핵심정리.md` 2개로 분리하되, 겹치는 개념(메시지 루프, 콜백, 이벤트 기반 구조 등)이 많아 한 번의 조사/작업 세션에서 같이 진행.
- 결과: `winapi_핵심정리.md`(13개 개념, 종합예제 TETORIS) + `mfc_핵심정리.md`(7개 개념, 종합예제 Lotto_peace) 완성. 두 파일이 서로의 항목을 번호로 상호 참조하도록 작성함(예: MFC 2번 메시지맵 ↔ WinAPI 3번 WndProc).

각 단계는: (a) 해당 언어 폴더 구조/개념 목차 초안 제시 → (b) 사용자 컨펌 → (c) 실제 md 작성, 순서로 진행한다. 한 번에 여러 언어를 동시에 진행하지 않는다.

## 각 단계별로 확정된 결정 사항 (시간순)

**1단계(C언어)**
- `C:\Study\C` 안에서 실제로 순수 C인 것: `C_ST/ST.cpp`, `File_ST/miro.cpp` → `c_핵심정리.md`에 포함.
- 실제로는 C++인 것(이름과 무관): `CPP_ST/`, `OOP/`, `TRPG_peace/`, `Wordle_c/` → `cpp_핵심정리.md`로 이월.
- `Critical_Section/`(스레드/임계구역, WinAPI `CreateThread`/`CRITICAL_SECTION` + 일부 C++11 `std::thread` 혼재) → C언어 파일에 개념 대표로 포함(사용자 확인).
- `File_ST/miro.cpp`(미로찾기, 스택을 배열로 직접구현 + 백트래킹)이 C 폴더 전체 대표 예제.

**2단계(C++)**
- `CPP_ST/`, `OOP/`, `Wordle_c/`(이름과 무관하게 실제 C++) → `cpp_핵심정리.md`에 포함.
- `TRPG_peace/`는 클래스 선언만 있고 메서드 본문이 전부 비어있는 미완성 스텁이라 정리에서 제외.
- `OOP/` 프로젝트(Account 상속 계층 + Container<T> 템플릿 + AccManager)를 C++ Part1 대표 예제로 선정.
- 코드 리뷰 중 실제 이슈 발견 → cpp_핵심정리.md 하단 "고쳐볼 만한 부분"에 기록: Account에 가상 소멸자 없음(Container가 기반포인터로 delete), mystring::operator=에 자기대입 가드 없음, override 키워드 미사용.
- `C:\Study\API\CPP_PLUS`(싱글톤, 스택/이중연결리스트 템플릿화, TCP·UDP 소켓, C++11 동시성 심화)를 "같은 시기에 배운 C++ 중급/고급 과정"으로 확인받아 `cpp_핵심정리.md` Part 2로 포함. `FMODENGIN` 헤더는 미사용이라 제외.

**3단계(WinAPI+MFC)**
- 소스: `C:\Study\API`(`CPP_PLUS` 제외) + `C:\Study\MFC`.
- `C:\Study\D3D\BOARD\Engine`은 사용자 개인 별도 프로젝트라 완전 제외. `D3D\D3D_ST`도 이번 단계에서는 다루지 않음(그래픽스/D3D 정리가 필요하면 나중에 별도 논의).
- `C:\Study\lookis` 폴더는 다른 교육기관에서 배운 내용이라 전체 제외.
- 결과: `winapi_핵심정리.md`(13개 개념, 종합예제 TETORIS) + `mfc_핵심정리.md`(7개 개념, 종합예제 Lotto_peace). 두 파일이 서로의 항목을 번호로 상호 참조(예: MFC 2번 메시지맵 ↔ WinAPI 3번 WndProc).

## 4단계(Unity) 진행 방식 (확정, 2026-07-27)
- **다른 언어와 달리 프로젝트 단위로 하나씩, 매번 보고 후 사용자 허락을 받고 다음 프로젝트로 진행.** 한 번에 여러 프로젝트를 정리하지 말 것.
- 순서는 프로젝트 폴더의 **생성일**(Windows `CreationTime`, `Get-Item ... | Select CreationTime`으로 확인) 순.
- 제외 폴더(사용자 확정): `ChainRiposte`, `DGC`, `PUZZLE`, `SoulsServer`, `assets`, `SuperMarioBros` — 전부 개인적으로 별도 진행한 프로젝트.
- 전체 순서와 진행 상태는 `unity_핵심정리.md` 최상단의 표에 기록되어 있음 — 새 세션에서는 그 표를 보고 다음 순번 프로젝트부터 이어가면 됨.
- Unity 프로젝트 안에는 C/C++/C# 문법 학습 내용도 섞여있을 수 있다고 사용자가 언급함 — 발견하면 unity_핵심정리.md에 넣을지 cpp_핵심정리.md/c_핵심정리.md를 보강할지 사용자에게 확인.
- 면접 준비 목적이므로 애매한 설명 없이 "명확하게" 정리할 것 — 사용자가 재차 강조함.
- WinAPI/MFC 정리에서 미리 걸어둔 연결고리: 델타타임 기반 게임 루프(winapi 7번 ↔ Unity Update/FixedUpdate), UI/로직 책임 분리(winapi 10번 ↔ MonoBehaviour vs 순수 C# 클래스), 종료 시 훅(mfc 6번 ↔ OnApplicationQuit) — 관련 프로젝트 정리할 때 실제로 이어줄 것.

## Unity 대상 목록 확장 (2026-07-27, 사용자 추가 요청)
사용자가 바탕화면(`C:\Users\user\Desktop`)에도 정리 대상 Unity 프로젝트가 있다고 알려줘서 확인함. 생성 시점이 불확실해 `unity_핵심정리.md` 목록 맨 뒤(21~25번)에 배치:
- **`Astar`**(A* 길찾기, 9개 스크립트) — 정상적인 학생 프로젝트.
- **`St5`**(3D 액션 게임, `PlayerCtrl.cs` 881줄/`EnemyCtrl.cs` 745줄 등 총 2829줄) — 지금까지 중 가장 큰 프로젝트. 여러 세션에 걸쳐 나눠 다룰 가능성 높음. St1~ST_2 계열과 겹치는 개념(카메라 추적, 체력 등)이 있을 수 있으니 새로운 내용 위주로 압축.
- **`우선순위HeapSort`**(힙 정렬/우선순위 큐, 2개 스크립트 295줄) — 정상적인 학생 프로젝트.
- **`FSM`(바탕화면)**: `Study\Unity\FSM`(16번)과 diff 확인 결과 사실상 동일 프로젝트 — Study 쪽이 인코딩 정상 + `OnStateExit` 구현이 추가된 더 완성된 버전. **바탕화면 FSM은 별도 처리하지 않고 16번에서 한 번만 다룸.**
- **`DressUpAsset`**: 학생 코드 없음, `dressup+assets.unitypackage`(서드파티 캐릭터 에셋) + `Ch2.DressUp.pdf`(ibatstudio 강의자료로 확인, 리스트/아바타 옷갈아입히기/Animator Humanoid 리깅/애니메이션 트랜지션/PlayerPrefs+씬전환 저장 다루는 튜토리얼). 사용자 확인: "PDF 개념을 요약해서 포함"으로 처리 — `unity_핵심정리.md`에서 "내 코드" 대신 "학습한 개념(출처: 강의자료)"로 명시.
- **`JSON`**: 마찬가지로 학생 코드 없음, PDF(`Ch6.JSON사용하기`, `서버프로그래밍01~03`) + 예제 zip(`JSONEx.zip` — 내부 타임스탬프 2017년, 강사 배포 예제로 추정) + `exam_tender.sql`(21MB DB 덤프, 무관한 샘플 데이터로 보임). 이것도 "학습한 개념" 방식으로 요약 예정 — 아직 PDF 상세 내용은 안 읽음, 해당 순번 도달 시 정독 필요.

## 5번 `3dTO2d` 제외 처리 (2026-07-27)
확인 결과 스크립트 7개(726줄) 전부가 Unity 공식 "Starter Assets - FirstPerson Character Controller" 패키지 그대로였고(`FirstPersonController.cs` 등), `IronSpear Content`라는 서드파티 환경 에셋도 포함 — 학생이 직접 짠 코드가 전혀 없어 정리 대상에서 제외. unity_핵심정리.md 표에 🚫 표시로 남겨둠.

## 6번 St3 — 규모가 커서 하위 분할 진행 중 (2026-07-27~)
St3(60개 스크립트, 6676줄)는 `Study1`~`Study7` 폴더별로 5단계(St3-A~E)로 나눠 진행하기로 확정. 세부 진행 상황은 `unity_핵심정리.md`의 "St3 하위 진행 표"에 기록.
- **St3-A**(`Study1`+`Study2`: 델리게이트/이벤트/람다/프로퍼티/싱글톤 개론) — ✅ 완료 (2026-07-27). 특히 `MonoBehaviour`를 `new`로 만들면 안 되는 이유를 직접 실험한 코드(`Property.cs`)가 좋은 발견이었음.
- **St3-B**(`Study3`+`Study4`: 자료구조/제네릭 싱글톤 진화/UI 이벤트 후킹) — ✅ 완료 (2026-07-27). `Study4/Singleton`의 `MonoSingleton1→2→3` 진화(자가참조 제네릭 `where T : MonoSingleton3<T>`, CRTP류 패턴)를 5-7/5-8번(St3-A)과 연결해서 정리함.
- **St3-C**(`Study5`: 에디터 확장 + AssetBundle) — ✅ 완료 (2026-07-27). `[MenuItem]`/`[CustomEditor]` 에디터 확장 + AssetBundle 빌드/로드, 그리고 `WWW`→`UnityWebRequest` API 마이그레이션 실사례(ST_2의 `Application.loadedLevel` 건에 이은 2번째 사례)를 다룸.
- **St3-D**(`Study6`: 인벤토리 시스템 + 커스텀 `PropertyAttribute`/`PropertyDrawer`(`ShowIfAttribute`) + 에디터 툴) — ✅ 완료 (2026-07-27). `ScriptableObject` 상속계층, 리플렉션 기반 `PropertyDrawer`, `EditorWindow` 심화, 세 번째 Deprecated API 사례(`[PreferenceItem]`→`[SettingsProvider]`)까지 다룸. `ItemManager`가 런타임 MonoBehaviour에서 에디터 전용 `AssetDatabase`를 잘못 쓰는 실제 버그도 발견.
- **St3-E**(`Study7`: 프로시저럴 메시 생성 + 커스텀 기즈모) — ✅ 완료 (2026-07-27). `OnDrawGizmos`, `Mesh` 직접 구성(vertices/uv/normals/triangles), 기즈모+메시 결합한 실시간 편집 종합예제, `[ContextMenu]`(4번째 에디터 확장 어트리뷰트), 메시를 에셋으로 저장하는 워크플로우까지 다룸.
- **St3 전체 완료 (2026-07-27).** `unity_핵심정리.md`에 Study1~7을 관통하는 "St3 종합" 요약 섹션도 추가함(MonoBehaviour new 제약의 파급효과, 문자열 기반 API의 반복된 위험성, 3번의 Deprecated API 마이그레이션, 에디터 확장 어트리뷰트 4종 세트 등).

## 7번 3D_ST1 완료 (2026-07-27)
스크립트 66개 중 대부분(`Ch2/Standard Assets`)이 Unity 공식 Standard Assets 패키지 + 서드파티 VFX/지형 에셋(`FT_MagicEffect_Vol04`, `atsV2`)이라 제외하고, 실제 학생 코드 7개 파일만 정리(`VideoPlayer` 인트로, `CharacterController` 이동, 레거시 `Animation`/`CrossFade`/`PlayQueued`, 래그돌 물리, 3D 충돌 데미지). `Ch3/Study1/AnimationEvevnt.cs`는 ST_2에서 이미 다룬 것과 동일해 각주 처리만 함.

## 8번 TempleRun 완료 (2026-07-27)
학생 코드는 `04. Scripts/csBridge.cs`, `csPlayer.cs` 2개뿐(엔드리스 러너). `Assets/unity-chan!`은 UTJ 공식 무료 캐릭터 에셋이라 제외. **원본 강의자료 `Ch4.TempleRun.pdf`(프로젝트 폴더 내 위치)를 학생 코드와 직접 대조**해서 정리를 보강함 — PDF에 담긴 강사의 기본 튜토리얼 코드와 학생의 최종 코드 사이의 실제 차이를 근거로 여러 통찰을 얻음:
- 카메라 추적은 학생이 짠 코드가 아니라 Standard Assets `Utility/SmoothFollow.cs`를 그대로 붙인 것(PDF로 확인 전에는 `04. Scripts`에 카메라 코드가 없어서 의문이었음) — `MaskCamera` 2-카메라 합성으로 바다 배경도 별도 처리.
- 태그 비교: PDF 원본은 전부 대문자(`"BRIDGE"` 등), 학생 코드는 `"Bridge"`(첫 글자만 대문자) — `ProjectSettings/TagManager.asset`으로 실제 태그명도 학생 코드와 일치함을 확인(버그 아님, 일관된 커스터마이징).
- 애니메이션: PDF 원본은 레거시 `Animation`(`anim.Play("jump_pose")` 등, `PrototypeCharacter` 사용)인데, 학생의 최종 코드는 Mecanim `Animator`(`SetInteger`/`SetTrigger`)로 바뀌어 있고 옛 코드는 주석으로 남아있음 — 씬 파일에서 캐릭터가 `unitychan`으로 교체된 것도 확인, 튜토리얼을 넘어선 직접 업그레이드.
- `speedForward`가 PDF 원본 2 → 학생 코드 10(5배)로 튜닝됨.
- `OnGUI`의 주석 처리된 `Application.LoadLevel("Main")`은 씬이 `"scGame"`으로 리네임된 뒤에도 옛 이름 그대로 남은 죽은 코드임을 PDF 대조로 확인.
- `SendMessage`/`StartCoroutine(string)` 같은 문자열 기반 API는 PDF 원본에도 있는 튜토리얼의 기본 패턴임을 확인(학생의 설계 실수가 아님).
정리한 개념(7-1~7-7): 2-카메라 합성(SmoothFollow), 엔드리스 러너 스트리밍 생성/삭제, Raycast 기반 접지/벽 판정, 문자열 기반 API 위험성(St3 테마와 연결), 모바일/데스크탑 입력 분기, 레거시 `Animation`→Mecanim `Animator` 업그레이드(3D_ST1과 대비), `OnGUI` 레거시 UI(ST_2의 `Application.LoadLevel` 흔적 재발견). 실제 버그/이슈 3건: `CheckMove`의 `isGround` 판정 로직 허점(PDF 원본에도 있는 튜토리얼 자체의 허점), `OnGUI`의 매 프레임 문자열 `Replace` 불필요 할당, 주석 속 죽은 `Application.LoadLevel("Main")`.

## 원본 강의자료(PDF) 대조 — 앞으로 매 프로젝트마다 확인할 것 (2026-07-27 확정)
TempleRun에서 원본 PDF(`Ch4.TempleRun.pdf`)를 학생 코드와 대조했더니 카메라 추적 코드의 정체, 태그 대소문자 일관성, 레거시→Mecanim 업그레이드 흔적, 값 튜닝(speedForward), 죽은 주석 코드 등 코드만 봐서는 알 수 없던 통찰을 여러 개 얻었다. **앞으로 각 프로젝트를 조사할 때 `find <프로젝트폴더> -iname "*.pdf"`로 원본 강의자료가 있는지부터 확인하고, 있으면 학생 코드와 대조해서 "PDF 원본과 비교" 형태로 반영할 것.** 확인 결과 최소 아래 프로젝트에 PDF가 있음(스캔 시점 2026-07-27, 내용은 미확인): `zombieStudy`(9번), `VR_meta`(13번), `URPassets3D`(15번), `Art`(20번). 해당 순번 도달 시 반드시 열어볼 것.

## 9번 zombieStudy 완료 (2026-07-28)
Photon PUN(Classic) 기반 멀티플레이어 좀비 슈팅. 스크립트 27개(4844줄) 중 `ParticleSystemMultiplier.cs`/`ExplosionPhysicsForce.cs`는 폴더 위치와 무관하게 `namespace UnityStandardAssets.Effects`(Standard Assets)라 제외. `MotorcyclePack_`/`Photon Unity Networking`/`PhotonChatApi`/`Standard Assets`도 서드파티/공식 패키지라 제외.
- **PDF 관련 정정**: 사전 스캔에서 "PDF 있음"으로 기록했던 것은 강의자료가 아니라 Photon SDK 자체 공식 문서(`PhotonNetwork-Documentation.pdf`)였음 — 대조할 원본 강의자료는 없었음. **13번 `VR_meta`, 15번 `URPassets3D`, 20번 `Art`도 같은 방식으로(파일명만 보고 "PDF 있음"이라 표시한 상태) 실제로 강의자료인지 재확인 필요** — 해당 순번 도달 시 열어보기 전에 내용부터 확인할 것.
- 처음으로 네트워킹(Photon PUN)을 다룬 프로젝트라 12개 개념(8-1~8-12) 전부 신규: PhotonView 소유권, RPC, OnPhotonSerializeView 커스텀 동기화, Master Client 권위 서버+페일오버, 소유권 vs 권위 두 가지 권한 모델 비교, 코루틴 기반 우선순위 FSM, 정수 상태코드 애니메이션 동기화, Raycast 터렛 자동조준, 레거시 Animation 인덱서 제어, AssetBundle WWW→UnityWebRequestAssetBundle 마이그레이션(4번째 사례), 전처리기 지시자 플랫폼 분기, SendMessage+object[] 다중인자 우회. 실제 버그 6건 발견(하단 "고쳐볼 만한 부분" 참고).

## 10번 VRstudy 완료 (2026-07-28)
Google Cardboard(스마트폰 거치형 VR) 소규모 실습. 학생 코드 6개 파일 378줄(`Event.cs`/`PlanesMaker.cs`/`VRBtn.cs` + `Zombie/` 폴더 4개)로 지금까지 중 가장 작은 축. `AwesomeCartoonPlanes/Scripts/Plane.cs`는 "FOR DEMO PURPOSES ONLY" 명시된 에셋 데모 스크립트라 제외, `Decrepit Dungeon LITE`는 스크립트 없는 모델 에셋, `Samples/Google Cardboard XR Plugin`은 구글 공식 샘플이라 제외. 이번에도 실제 강의 PDF는 없었음(패키지 캐시 워크시트만 있음 — zombieStudy와 같은 패턴).
- 7개 개념(9-1~9-7): VR 시선 기반 포인터 이벤트, 응시시간(Dwell Time) 게이지 UI(VRBtn/GateController 2가지 적용), 오브젝트 재활용 패턴(TempleRun 7-2번과 대비되는 실제 풀링 구현 사례), 시선 방향 기반 이동+멀미방지 정지각, 에디터 전용 프로토타입 입력(zombieStudy 8-11 연장), 거리 기반 애니메이터 블렌딩(zombieStudy 8-6 복잡한 FSM과 대비되는 미니멀 AI), 플랫폼별 절전모드 방지(zombieStudy 8-11과 연결). 이슈 2건 발견.

## 11번 ARstudy 완료 (2026-07-28)
Vuforia Engine 기반 AR 실습. 특이하게 한 프로젝트 안에 시기가 다른 두 세대 코드가 공존: (구) Image Target 카드 배틀(`ARCardBattle.cs`/`TrackingObject.cs`/`Finder.cs`, 레거시 OnGUI) vs (신) Ground Plane + New Input System + GPS 몬스터 캐치(`AutoGroundPlacer.cs`/`ARGravityController.cs`/`CatchBall.cs`/`UpdateGPSLocation.cs`). 여기에 Vuforia 없이 웹캠+자이로로 만든 DIY AR 실험(`TestWebCam.cs`)까지 있어 "손수 구현→SDK 이미지 인식→SDK 평면 인식"의 학습 진행을 한 프로젝트에서 볼 수 있었음. 제외: `Fantasy Monster(wizard) Demo`의 데모 스크립트 2개(에셋 팩), `GameManager.cs`(빈 스텁), Vuforia 자동생성 마이그레이션 스크립트.
- 8개 개념(10-1~10-8): Image Target 카드 배틀, Vuforia 타겟 인식 콜백(OnTargetFound/Lost/OnContentPlaced), Ground Plane 자동/수동 배치+UnityEvent 리스너, 타겟 기울기→물리 낙하 트리거(내적 활용), 레거시 Input Manager→New Input System 마이그레이션(가장 큰 규모의 API 교체 사례), GPS 근접 알림+구면거리계산, 웹캠+자이로 DIY AR, 레거시 OnGUI(TempleRun 7-7과 연결, 같은 프로젝트 안에서 신/구 UI 세대차 대조 가능). 이슈 2건.
- 이번에도 PDF는 없었음(Vuforia 에뮬레이터 안내서만 존재) — zombieStudy/VRstudy에 이어 3번째로 "패키지 캐시에 PDF가 있다고 파일명만 보고 오판하지 않기"가 재확인됨.

## 12번 ModelURP 제외 처리 (2026-07-28)
학생 스크립트가 전혀 없음 — `Assets`를 전체 검색해도 `.cs` 파일은 `TutorialInfo/Scripts/Readme.cs`·`ReadmeEditor.cs` 2개뿐이며, 둘 다 Unity URP 템플릿이 프로젝트 생성 시 기본으로 넣어주는 튜토리얼 안내판 보일러플레이트다. 나머지는 `Glavenus`/`Nergigante`(몬스터헌터 팬 3D 모델 에셋)와 `Settings`(URP 렌더 파이프라인 설정)뿐 — 코드 실습이 아니라 3D 모델을 URP에서 임포트/셰이딩 테스트해본 프로젝트로 보인다. 강의 PDF도 없음. 3dTO2d(5번)와 같은 사유로 정리 대상에서 제외.

## 13번 VR_meta 완료 (2026-07-28)
Meta Quest VR, XR Interaction Toolkit(XRI) 기반. 학생 코드 3개 파일 355줄(`CustomDirectInteractor.cs`/`CustomGrabInteractable.cs`/`LogMessage.cs`). 사전 스캔의 "PDF 있음" 표시는 이번에도 오판(TextMesh Pro 자체 사용자 가이드였음) — zombieStudy·ARstudy에 이은 3번째 같은 사례.
- 4개 개념(11-1~11-4): XRI API 마이그레이션(파라미터 기반→EventArgs 기반, 오버라이드 지점 4~5곳이 구/신 버전 쌍으로 나란히 남아있어 지금까지 중 가장 촘촘한 마이그레이션 대조 사례), XRDirectInteractor 확장(잡은 오브젝트 정보 토글), XRGrabInteractable 확장(왼손/오른손 다른 어태치 포인트), Select vs Activate 이중 입력 체계.
- 특이사항: 이번엔 "고쳐볼 만한 부분"이 아니라 **학생이 이미 스스로 고친 버그**를 발견 — `GetChild(0) != null`(자식 없으면 예외를 던지는데 null 체크로 방어하려던 구버전 실수)를 `childCount > 0`으로 직접 수정해 두 스크립트에 일관되게 반영한 것을 확인, 별도 절로 기록.

## 14번 UnityDll 완료 (2026-07-29)
Unity 네이티브 플러그인(P/Invoke) + 순수 C# 플러그인 DLL 실습. Unity 프로젝트 안 스크립트는 3개뿐(`Study1.cs`/`Study2.cs`(빈)/`Study3.cs`)이지만, **Assets/Plugins의 DLL 2개(`UnityDllCpp.dll`, `UnityDllCs.dll`)를 만든 원본 소스가 Unity 프로젝트 바깥의 별도 VS 프로젝트로 존재**함을 발견해서 같이 조사함: `C:\Study\UnityDllCpp`(C++ DLL 소스), `C:\Study\UnityDllCs`(C# 클래스 라이브러리 소스). 씬 파일(`SampleScene.unity`)도 guid 대조로 직접 확인해서 "Main Camera에 YComponent를 실제로 Add Component 했다"는 에디터 작업 사실까지 반영함 — 사용자가 "코드도 코드지만 유니티 상에서 작업한 내용도 중요하다"고 명시적으로 요청한 부분.
- 패키지 캐시 PDF(Code Coverage 툴 워크시트)는 이번에도 강의자료 아님으로 확인 — zombieStudy/ARstudy/VR_meta에 이은 4번째 같은 오판 방지 사례.
- 8개 개념(12-1~12-8): P/Invoke 기본기(`extern "C"`+`DllImport`), DLL 전역 상태 생명주기, `IntPtr`+`unsafe`+`fixed` 포인터 마샬링, **(발견) C++엔 구현됐지만 Unity 스크립트에서 호출 안 하는 구조체/2차원배열 마샬링(`GetNum5`/`GetNum6`)** — 배운 개념과 실제 연결 개념 사이 간극을 보여주는 사례, 순수 C# 플러그인 DLL 제작(`MonoBehaviour`를 DLL로 빌드), 씬 파일 guid 대조로 확인한 실제 에디터 부착 내역, Inspector 직렬화 규칙(`[System.Serializable]`+다차원 배열 미노출), C# 배열 문법(1차원/다차원/가변길이) 총정리.

## 15번 URPassets3D 제외 처리 (2026-07-29)
학생 스크립트 없음 — 전부 서드파티/템플릿(`Mirza Beig Cinematic Explosions FREE`, `UnityTechnologies ParticlePack`, `TutorialInfo` URP 보일러플레이트) + 무료 3D/환경 에셋(`LeartesStudios FantasyCastle`, `Fantasy Skybox FREE`, `SkySeries Freebie`) 임포트뿐. 씬도 기본 샘플 씬 그대로. 12번 ModelURP와 동일 사유로 제외. PDF(TextMesh Pro 가이드)도 강의자료 아님 확인(5번째 같은 오판 방지 사례).

## 16번 FSM 완료 (2026-07-29)
FSM을 세 가지 방식(절차적 if/else → 순수 C# State 패턴 → Unity Animator `StateMachineBehaviour`)으로 순서대로 다시 구현해본 진화 과정이 한 프로젝트에 남아있어 지금까지 중 가장 명확한 "같은 문제를 여러 기법으로 풀어본" 비교 사례였음. `EnemyController_1~3_2.controller` 5개 버전으로 Animator 그래프 자체도 반복 개선한 흔적 확인. 에디터 확장도 2건 발견: 커스텀 Script Template(FSM 상태 클래스 보일러플레이트 자동 생성)과 커스텀 키보드 단축키(`%&n`=Ctrl+Alt+N으로 스크립트 생성). `Assets/Scripts/` 폴더의 중복 파일들은 diff 결과 중간에 잘린 구버전임을 확인하고 제외, 루트 버전만 사용.
- 8개 개념(13-1~13-8). 바탕화면 `FSM`은 기존 결정대로 이 프로젝트와 동일해서 별도 처리 안 함.
- **교훈**: 같은 이름의 파일이 여러 경로에 있을 때(`Assets/` 루트 vs `Assets/Scripts/`) 반드시 diff로 대조하고 최종본을 판별할 것 — 이번에 실제로 구버전을 대표 예제로 쓸 뻔했다가 diff로 잡아냄.

## 17번 ShaderST 완료 (2026-07-29)
C# 스크립트가 전혀 없고 `.shader` 파일 5개(고정함수 `Deep+`/`Deep-`, CG Surface Shader `Lambert_Old`, URP HLSL `Lambert`, 학습노트 `StudyShader.Shader`)만 있는 순수 셰이더 문법 실습. `StudyShader.Shader`는 실제 컴파일 대상이 아니라 학생이 ShaderLab 구조/Queue/Pass 단계를 스스로 정리한 학습노트였음 — 다른 프로젝트의 "강의 PDF 대조" 역할을 학생 본인이 쓴 필기가 대신한 특이 케이스. 씬에 배치된 Cube/Quad 오브젝트로 RenderQueue 그리기 순서 실험도 실제로 해봤음을 확인.
- 6개 개념(14-1~14-6): ShaderLab 3단 구조+Tag, RenderQueue로 그리기 순서 제어(ZWrite Off 전제), 레거시 고정함수 파이프라인, CG Surface Shader Lambert 라이팅+노멀맵(+발견: 직접 재구현한 커스텀 라이팅 함수가 실제로는 안 쓰임, UnityDll 12-4와 같은 패턴), Built-in RP→URP HLSL 마이그레이션 실사례(지금까지와 다른 축 — 스크립팅 API가 아니라 렌더 파이프라인 자체 전환), C# 스크립트 전무하다는 점 자체 기록.
- PDF는 이번에도 패키지 캐시 워크시트(6번째 같은 오판 방지 사례).

## 18번 Localization 완료 (2026-07-29)
다국어 로컬라이제이션 시스템을 두 세대(싱글톤 MonoBehaviour+리스트 인덱스 → 정적 클래스+CSV Dictionary)에 걸쳐 다시 짠 실습. `LocalizationTool.unitypackage`를 다운받아 기반으로 삼고 구글 시트로 번역 원본을 관리했다는 에디터 작업 메모(`260714_개발.txt`, `예제.txt`)를 발견해 출처까지 확인. 씬 대조 결과 `GUITest`/`Text (Legacy)` 오브젝트에 리팩터링 전/후 버전 스크립트가 동시에 붙어있음을 확인(FSM의 Animator Controller 버전 보존과 같은 패턴).
- 8개 개념(15-1~15-8): 아키텍처 진화(인스턴스+리스트→정적+Dictionary), CSV 기반 텍스트 관리(구글시트→CSV→Resources), 델리게이트 이벤트로 언어변경 전파(값 직접 전달→신호만 전달로 설계 개선), **(발견) 이미지는 3개 언어 지원하는데 텍스트 CSV는 2개 언어뿐인 불일치**, 커스텀 PropertyDrawer(St3-D와 연결), CSVReader 동기/비동기 이중 지원(코루틴+async/await, `#if UNITY_2020_1_OR_NEWER` 조건부 컴파일), 씬에서 확인한 신구 버전 공존, 범용 CSV 데이터 테이블로 확장(번역표 외 게임 밸런스 데이터에도 재사용).
- PDF는 없었음(대신 에디터 작업 메모 txt 파일 2개로 출처 확인).

## 19번 Crypt 완료 (2026-07-29)
저장 시스템(BinaryWriter→StreamWriter 평문→JsonUtility+암호화)과 암호화 기법(AES ECB→AES CBC+매번 새 IV) 각각을 여러 세대에 걸쳐 발전시킨 실습. `Downloader.cs`는 별도 주제(구글 시트 아이템 테이블 다운로드, Localization과 다른 URL 패턴으로 재실습)로 같이 들어있음. 씬 대조 결과 `Player` 오브젝트에 3세대 저장 스크립트가 동시 부착(FSM/Localization에 이은 3번째 신구 공존 패턴).
- 8개 개념(16-1~16-8): 저장 시스템 3단계 진화, `Application.dataPath` vs `persistentDataPath`, AES ECB 모드(1세대, 취약점을 학생이 직접 주석으로 인지), AES CBC+매번 새 IV(2세대, 길이 검증 예외처리까지 추가), Save 데이터 보안 3단계 실험(원문→Base64→암호화, 인코딩과 암호화를 정확히 구분), **암호화 키 하드코딩을 학생 스스로 한계로 인지한 주석**, 구글 시트 CSV 다운로드 재실습(gid 파라미터), 씬에서 확인한 신구 저장 시스템 공존.
- PDF는 이번에도 패키지 캐시 워크시트(7번째 같은 오판 방지 사례).

## 20번 Art 완료 (2026-07-29)
사용자가 "코드보다 아트쪽(애니메이션/활용법/서드파티)이 중심"이라고 직접 방향을 지정해준 프로젝트. 스크립트는 `PlayerController.cs`(20줄)/`ChangeSprite.cs`(9줄) 2개뿐, 실제 내용은 3개 모듈: `00_Character_Complete`(PSB 레이어 기반 2D 캐릭터 파츠 리깅 + `SpriteLibraryAsset`로 스킨 교체), `01_Ui_Complete`(Animator 기반 UI 버튼 4상태 애니메이션 + 구글 폰트를 TextMeshPro SDF로 변환), `02_ParticleSystem_Complete`(파티클 모듈별 스터디 그리드 + 상자 오픈/레벨업 UI 이펙트 합성).
- 8개 개념(17-1~17-8): Sprite Library 스킨 교체, Animator로 캐릭터 이동/공격 구성(FSM 13-3과 같은 책임분리 패턴 재확인), Animator 기반 UI 버튼 상태 애니메이션, 상자 오픈 연출(Animator+파티클 5종 합성), 파티클 모듈별 스터디 그리드(SimulationSpace/SortMode/Emission/Noise/Sub Emitter 등), 경험치/레벨업 UI 이펙트(상자 오픈과 다른 결의 UI+이펙트 합성), TextMeshPro SDF 폰트(서드파티 구글 폰트 활용), 코드보다 아트 작업이 중심이라는 프로젝트 성격 자체 기록.
- **PDF 결론**: 00_작업계획.md에서 "진짜 강의자료일 가능성"으로 남겨뒀던 마지막 항목이었는데, 실제로 열어본 결과 이번에도 TextMesh Pro 자체 가이드+Code Coverage 워크시트로 확인 — "패키지에 딸려온 PDF ≠ 강의자료" 패턴이 8번째로 재확인되며 이 정리 작업 전체에서 완전히 결론남(더 이상 프로젝트별로 이 가능성을 따로 의심할 필요 없음).

## 21번 Astar 완료 (2026-07-29)
그리드 기반 A* 길찾기를 직접 구현한 실습(스크립트 9개, 1347줄). `PathFinding.cs`(601줄)에 F/G/H 비용, Open/Closed 리스트, 옥타일 거리 휴리스틱 등 교과서적인 A* 로직이 한국어 주석으로 매우 촘촘하게 설명돼 있고, 실행 방식이 동기(`FindPath`, 참고용) → 수동 스텝(`Step`, 안씀) → 코루틴 시각화(`FindPathCoroutine`/`IEStep`, 최종 채택) 3단계로 진화한 흔적이 한 파일에 남아있음. 재귀 대신 코루틴을 쓴 이유("재귀 함수 호출의 제한으로 인해 프로그램이 강제 종료 될 수 있다")도 주석에 명시.
- 8개 개념(18-1~18-8): A* 핵심 자료구조(F/G/H+Open/Closed), 옥타일 거리 휴리스틱, Open/Closed 갱신 메인 루프, 경로 역추적(RetracePath+Reverse), 알고리즘 실행 방식 3단계 진화, 탐색 과정 시각화(노드 색상+F/G/H 텍스트), 그리드 클릭 상호작용(좌/우클릭+W+R), **(참고) `PairNode`/`IEquatable` 실행 대기열 미사용 확장 설계**(UnityDll 12-4/ShaderST 14-4와 같은 패턴).
- PDF는 없었음.

## 22번 St5 — 별도 정리 없이 종료 처리 (2026-07-29)
사용자가 "예전에 했던 것일 수도 있으니 가볍게 훑어보고 중복이면 스킵"이라고 요청해서 조사한 결과: `St5/Assets/03. Scripts`(스크립트 18개) 중 15개가 zombieStudy(8번)의 `03. Scripts`와 **파일명이 완전히 동일**(`BaseCtrl`/`EnemyCtrl`/`PlayerCtrl`/`StageManager`/`BulletCtrl`/`EnemyWeapon`/`EnemyLife`/`ExplosionObject`/`SmoothFollowCam`/`DestructionRay`/`Billboard`/`CreateGizmo`/`SelectGizmo`/`RoamingBox`/`AutoDestroy`)이고, 터렛 자동조준(8-8)·코루틴 우선순위 FSM(8-6)·전처리기 지시자(8-11) 등 핵심 로직도 zombieStudy에 이미 상세히 문서화되어 있어 새로 쓸 내용이 없었음. `St5`는 Photon 멀티플레이어화 이전의 더 작은 단일 플레이어 스냅샷으로 판단됨. `PlayerCtrl.cs`는 한 걸음 더 거슬러 올라가 St3-A(5-1/5-3번)의 학습용 스텁과도 뒤쪽 코드가 동일 — St3(스텁) → St5(단일플레이어 완성형) → zombieStudy(Photon화) 계보로 확인. St5에만 있는 3개 파일(`Destructor`/`DoorCtrl`/`PlayerMoveCtrl`)도 이미 여러 프로젝트에서 다룬 기초 패턴이라 추가할 내용 없음.
- **처리**: 새 프로젝트 섹션을 만들지 않고, zombieStudy(8번) 섹션 상단에 이 발견을 설명하는 노트만 추가. 상단 표 22번 행도 "🔗 별도 정리 안 함"으로 갱신.

## 23번 우선순위HeapSort 완료 (2026-07-29)
배열 기반 이진 힙(Binary Heap)으로 Min/Max 우선순위 큐를 구현하고 트리 구조를 실시간 시각화하는 소규모 실습(스크립트 2개, 296줄, 겹치는 프로젝트 없음 확인). 6개 개념(19-1~19-6): 배열 기반 완전이진트리, 삽입+상향 재정렬(Sift-Up), 삭제+하향 재정렬(Sift-Down), Min/Max Heap 전환(비교 부등호만 다름), 힙 구조 실시간 시각화(GameObject 동적 생성+좌표 계산식), (참고) 배열 인덱스와 포인터(Node.left/right) 두 방식을 함께 쓰는 절충 설계. PDF 없음.

## 26번 다차원배열에디터 완료 — Unity 프로젝트별 정리 전체 완료 (2026-07-29)
`Assets/설명서.txt` 최상단에 "강사 선물"이라고 명시되어 있어 확인한 결과 **학생이 짠 코드가 아니라 강사가 완성해서 준 예제**로 판명(사용자 확인) — DressUpAsset/JSON과 같은 방식으로 "학습한 개념(출처: 강사 예제)" 형식으로 정리함. St3-C/D(에디터 확장 테마)와 가벼운 중복 체크 결과 주제는 겹치지만 실제 기법(SerializedProperty 직접 조작, Event 기반 드래그 페인팅, EditorWindow 드래그앤드롭, Undo+Dirty 짝지어 호출)은 겹치지 않아 새 섹션으로 작성. 특히 12-7번(UnityDll)에서 "다차원 배열은 인스펙터에 기본 노출 안 됨, 커스텀으로 노출시킬 수 있지만 아직은 참자"라고 미뤄뒀던 문제를 `List<bool>`+`index=y*width+x` 방식으로 정확히 해결하는 구조라 좋은 콜백이 됨. 6개 개념(22-1~22-6)으로 정리.
- **Unity 4단계(프로젝트별 정리) 전체 완료.** `unity_핵심정리.md` 상단 표 1~26번 전부 ✅/🚫/🔗 처리됨.
- **다음 단계(전체 계획의 5번)는 5개 파일(c/cpp/winapi/mfc/unity) 관통 종합 정리인데, 사용자가 다른 모델로 직접 진행할 예정 — 자동으로 시작하지 말 것.** 사용자가 명시적으로 요청할 때까지 대기.

## 24번 DressUpAsset 완료 (2026-07-29)
학생 코드 없음(다운로드한 unitypackage + `Ch2.DressUp.pdf` 강의자료, ibatstudio.com 워터마크로 출처 재확인). PDF 24페이지 전체를 정독해서 "학습한 개념(출처: 강의자료)"으로 6개 항목(20-1~20-6) 작성: `List<T>` 동적 컬렉션, 드레스업 "전부 끄고 하나만 켠다" 패턴(MakeDresses/ShowDress), Humanoid 아바타 리깅(Animation Type 4종+Configure+Muscle Preview), Animator 파라미터 트랜지션+Has Exit Time, UI Button+static 카운터 순환 교체, PlayerPrefs+SceneManager.LoadScene 씬 전환 저장. 강의자료 코드 자체의 허점(`PlayerPrefs.GetInt(...) == null` 비교가 항상 false로 평가되는 버그, HasKey를 썼어야 함)도 20-6번에 지적해둠.

## 25번 JSON 완료 (2026-07-29)
학생 코드 없음(강의자료 PDF `Ch6.JSON사용하기.pdf`="서버프로그래밍 G02.JSON활용" + 강사 배포 예제 `JSONEx.zip`). PDF 본문에 "Unity5 C#스크립트 코드는 생략하였다"고 명시되어 있어, 실제 코드는 `JSONEx.zip`(99.JSONEx 폴더, 2017년 타임스탬프)을 직접 압축 해제해서 확인 — 이 과정에서 PDF엔 없던 로그인/회원가입/로비 씬(`csMemberLogin.cs`/`csMemberJoin.cs`)까지 추가로 발견했다. `exam_tender.zip`(21MB SQL 덤프)은 기존 판단대로 무관한 샘플 데이터라 제외, `서버프로그래밍01~03` PDF는 APM/MySQL/웹서버 포팅 등 서버 인프라 문서라 Unity 개념과 무관해 이번 정리 범위에서 제외.
- 6개 개념(21-1~21-6, "학습한 개념(출처: 강의자료+예제코드)"): SimpleJSON 파서로 로컬 JSON 읽기, WWWForm+WWW로 서버 조회, WWWForm 데이터 전송+서버 응답 규약(returnCode/returnMsg), PHP 서버사이드 JSON 인코딩+UTF-8/EUC-KR 문자셋 변환(iconv), 로그인/회원가입 흐름(클라이언트 1차 검증+서버 2차 인증, 예제코드에서만 발견), **(참고) 레거시 WWW/WWWForm API** — St3(5-21)/zombieStudy(8-10)/Localization(15-6)에서 반복 확인한 WWW→UnityWebRequest 마이그레이션 테마의 2017년 시점 실물 근거 자료로 연결.

## 20번 Art 추가 작업분 반영 (2026-07-31)
사용자가 "Unity/Art쪽에 추가사항 있음"이라고 알려줘서 재조사. 20번 Art를 정리한 뒤(2026-07-29) **2026-07-30에 사용자가 새 모듈 `02_ExpAttractor_Complete`(햄스터 경험치 흡수 연출)를 만들고 상자 씬도 갱신**한 것이 확인됨(`Assets` 하위 7/29 이후 변경 파일 17개, 다른 Unity 프로젝트는 `ChainRiposte`(제외 대상)만 변경).
- **가장 큰 변화**: `Packages/manifest.json`에 **서드파티 uGUI 파티클 패키지가 git URL로 추가**됨 — `com.coffee.ui-particle`(mob-sakai ParticleEffectForUGUI, 버전 미고정)과 `com.coffee.ui-effect`(UIEffect `#5.9.0`, **설치만 되고 씬에서 실사용 0건**). Screen Space-Overlay 캔버스에서 파티클이 UI 뒤로 밀리는 문제를 `UIParticle` 컴포넌트로 해결한 것이 이번 작업의 핵심.
- 씬 YAML 대조로 확인한 실제 동작 흐름: `btn_exp` 클릭 → `UIParticle.Play()` → 경험치 구슬(0.3초 원샷, Velocity+Trail) 방출 → 게이지 중앙 `UIParticleAttractor`(DestinationRadius 0.1 / DelayRate 0.6 / MaxSpeed 2 / Smooth)가 흡수 → 구슬 도착마다 `OnAttracted` UnityEvent가 `ExpGain.GainExperience()` + `Animator.SetTrigger("Gain")` 동시 호출 → 게이지 가득 차면 `SetTrigger("Complete")` → `UI_Exp_Complete` 레벨업 연출.
- **작성**: 신규 개념 6개(17-9 UIParticle / 17-10 git URL 패키지 설치 / 17-11 UIParticleAttractor 흡수 연출 + 하이어라키 전체 / 17-12 OnAttracted UnityEvent + `ExpGain.cs` 분석 / 17-13 `m_IsActive` 커브 연출 오케스트레이션 + AnyState 2단 트리거 / 17-14 파티클 3종 역할분담 + UI/Default vs UI/UIAdditive 셰이더 선택).
- **정정 3건**: 17-4(상자 컨트롤러 2상태→4상태 Idle/None/Down/Open, 버튼 2개가 SetTrigger만 호출해 C# 0줄, 상자 씬에도 UIParticle 3개), 17-6(텍스처만 있던 단계의 기록임을 명시하고 17-11~13으로 연결), 17-8(스크립트 2개→3개 정정하되 "아트 중심" 결론은 오히려 강화), 섹션 헤더 + 상단 표 20번 행.
- **지적한 결함 2건**: (a) `ExpGain.cs`가 게이지 1f 도달 후에도 계속 호출되면 `Complete` 트리거가 중복 발사됨(리셋/플래그 없음) — AnyState 전이라 연출이 겹쳐 재생됨, (b) `com.coffee.ui-particle`의 git URL에 버전 태그가 없어 재현성 위험.
- **기존 번호(17-1~17-8)는 그대로 유지**했다 — `면접정리예제/03_Unity.md`(17-3/17-5/17-7)와 `05_그래픽스_수학.md`(17-1)가 이 번호를 참조 중이라 재번호 매기면 상호참조가 깨진다. 앞으로도 이미 정리된 섹션에 내용을 추가할 때는 **뒤에 이어붙이고 기존 번호는 건드리지 말 것.**
- 강의 PDF는 이번에도 없음(TextMesh Pro 패키지 문서뿐 — 기존 판단 그대로).

## 새 단계 — 교안 웹사이트 제작 (2026-08-04 시작)

> **⚠ 이 단계의 최신 기준은 `교안/00_인수인계.md`다.** 교안 작업을 이어받을 때는 그 파일을 먼저 읽을 것.
> 아래는 시작 시점의 요약이며, 진행 상황·챕터별 계획·검증된 사실은 전부 인수인계 문서에 있다.

기존 md 5개 + `면접정리예제/`는 **이미 아는 사람이 복기하는 자료**라서, 개념 정의가 한 줄이고 바로
"내 코드에서 어떻게 썼는지"로 넘어간다. 사용자가 **"처음부터 공부할 정도로 자세한 교안"** 이 필요하다고
지적해서, `교안/` 폴더에 정적 웹사이트를 새로 만들기로 했다.

- **결과물 형태(사용자 확정)**: 로컬 멀티페이지 정적 사이트. 외부 의존 0, 브라우저로 바로 열림.
- **진행 방식(사용자 확정)**: **챕터 하나씩 확인받고 다음으로.** 세션을 나눠서 진행한다.
- **서술 원칙(사용자 지시)**:
  - 코드를 한 줄씩 해설하지 말 것 — 코딩 자체는 AI가 하는 시대라 **CS 지식과 도메인 지식**이 중요.
  - 면접 답변용이 아니라 **기초 + 원리**를 이해할 수 있게 서술.
  - **중복 서술 금지**: 개념 정의는 study 챕터에만 두고, 나머지는 링크(`.seealso`)로 연결.
  - 면접 페이지는 답변만 두고 원리는 전부 study로 링크.

### 폴더 구조
```
교안/
  index.html            메인 허브 (전체 지도 + 진도 표시)
  assets/site.js        ★ 전체 목차 데이터(SITE 객체) + 공통 렌더링. 챕터 추가/상태변경은 여기서
  assets/style.css      공통 스타일 (라이트/다크, 콜아웃 .box.core/why/warn/game/iv, .diagram SVG)
  study/                20개 챕터 (6파트 A~F)
  interview/            면접 204문항 이식 예정
  projects/             내 코드 사례 (기존 md는 삭제하지 않고 원본으로 유지·링크)
  quiz.html             기존 09_퀴즈페이지.html 연결(추후 흡수)
```
- `file://`로 열어야 하므로 JSON+fetch 대신 `site.js`의 전역 `SITE` 객체를 쓴다.
- 챕터를 완성하면 `site.js`의 해당 항목 `status`를 `"todo"` → `"done"`으로 바꿔야 카드가 활성화된다.
- 진도/테마는 `localStorage`(`gyoan.progress.v1`, `gyoan.theme`)에 저장.

### 챕터 목록 (6파트 20챕터)
- **A 메모리와 언어 기반**: A-1 메모리 구조 / A-2 포인터와 참조 / A-3 동적 할당의 진짜 비용 / A-4 객체지향의 메모리 모델
- **B 자료구조·알고리즘**: B-1 배열 vs 연결리스트 / B-2 스택·큐·힙·해시 / B-3 탐색과 길찾기 / B-4 복잡도와 정렬
- **C 게임 런타임 구조**: C-1 게임 루프와 시간 / **C-2 오브젝트 관리와 메모리 풀 ✅** / C-3 컴포넌트 구조와 업데이트 순서
- **D C#/Unity 런타임**: D-1 값/참조 타입 / D-2 가비지 컬렉션 / D-3 Unity 엔진 구조
- **E 그래픽스·수학**: E-1 렌더링 파이프라인 / E-2 게임 수학 / E-3 셰이더와 드로우 콜
- **F 시스템·설계**: F-1 프로세스·스레드·동기화 / F-2 네트워크와 동기화 모델 / F-3 디자인 패턴

### C-2 완료 (2026-08-04) — 첫 완성 챕터, 서술 톤의 기준
바탕화면 `TTT`(탄막 슈팅 오브젝트 관리 뼈대)를 소재로 삼아 작성. 구성:
문제(프레임 시간의 일정함) → 풀의 원리 → 설계 3축 → TTT 실물 해부 → `operator new` 오버로딩 →
생성자/소멸자 RAII 등록 → 순회 중 삭제(반복자 무효화) → 다형성의 대가와 ECS → 결함 10건 → 30초 요약.
SVG 도해 3개(버퍼 슬라이스+두 리스트 / `new` 2단계 / 커서 후퇴 3단계) 포함.

**TTT 조사 결과 (다시 조사하지 말 것)**
- 학생 코드가 아니라 **외부에서 받은 예제**로 판단. 근거: 빌드 산출물
  `TTT.Build.CppClean.log`에 원본 경로 `c:\users\dnvmf\onedrive\project\wooprogameproject\...`(다른 사용자
  계정, vc142=VS2019)가 남아있음. 소스 타임스탬프는 2024-03(국비과정 폴더들보다 이전).
  이후 `C:\Study\TTT`에서 v143으로 재빌드 → 2026-07-31에 바탕화면으로 복사됨(`C:\Study\TTT`는 현재 없음).
- 코드 곳곳의 `std::cout` 추적 출력(`"New"`, `"테스크 생성"` 등)은 **동작 순서를 눈으로 확인하려고 넣은 학습 흔적**.
- **실측한 크기(MSVC x64, 직접 컴파일해서 확인)**: `CTask` 24 / `CMover`·`CEnemy` 56 / `CMyShip` 64 /
  `CBullet` 72, 정렬 전부 8. 그런데 세 리스트 모두 슬롯 크기를 **100**으로 잡아서
  **홀수 슬롯의 8바이트 정렬이 깨진다(100 % 8 == 4)** — 발견한 결함 중 가장 심각. 면접에서 쓸 만한 소재.
- 그 외 결함: `Alive`를 false로 만드는 코드가 없어 아무것도 안 죽음 / `EnemyList`·`BulletList` 미사용 /
  풀 고갈 시 `New`가 NULL 반환(비-noexcept `operator new`의 규약 위반 → 생성자가 널에서 실행) /
  `~CTaskList`가 살아있는 태스크 소멸자 미호출 / `CTask::operator new` 본문이 빔(`= delete`가 정석) /
  센티넬이 생성되지 않은 raw 메모리(vptr 미초기화) / `void main()`(실제 경고 C4326 확인) /
  `CMyShipCrash` 인스턴스화 불가 / 전역 `MyShip` 포인터 미정리.

### 다음 챕터
사용자 확인 후 하나씩. 순서는 **A-3 → A-4 → B-1 → C-1** 을 먼저 하는 것을 권장 —
C-2가 이 네 곳으로 `.seealso` 링크를 이미 걸어놨기 때문에 링크 대상이 빨리 생긴다.
면접 204문항 이식과 퀴즈 흡수는 study 챕터가 어느 정도 찬 뒤에 진행한다.

---

## 다음 세션에서 할 일
**Unity 프로젝트별 정리는 26번 `다차원배열에디터`를 끝으로 전체 완료됨(2026-07-29).** 이제 전체 계획(위 "진행 순서" 1~5번)에서 남은 건 5번 "5개 파일(c/cpp/winapi/mfc/unity) 관통 종합 정리" 하나뿐인데, **이건 사용자가 다른 모델로 직접 진행할 예정이므로 자동으로 시작하지 말고 사용자의 명시적 지시를 기다릴 것.** 사용자가 이 세션에서 그 종합 정리를 요청하면 그때 시작한다.
같은 이름의 스크립트가 프로젝트 안 여러 경로에 중복 존재하면 diff로 대조해서 최종본을 판별할 것(FSM에서 `Assets/Scripts/`가 잘린 구버전으로 판명된 사례 — 16번 FSM 항목 참고).
**중요(2026-07-29 확정)**: 코드뿐 아니라 유니티 에디터 상에서 실제로 한 작업(씬에 어떤 컴포넌트를 어디 붙였는지, 인스펙터 세팅 등)도 조사에 포함시킬 것 — 필요하면 `.unity` 씬 파일을 열어 `MonoBehaviour` 블록의 `m_Script` guid와 `.meta` 파일 guid를 대조해서 확인한다(UnityDll에서 처음 시도, Main Camera에 YComponent가 실제로 붙어있음을 이 방법으로 검증함).
참고1: `ST_2`에서 "9. Etc" 폴더처럼 프로젝트 안에 서드파티 에셋(UltimateJoystick)과 성격이 다른 도구(에디터 확장 등)가 섞여있는 경우가 또 나올 수 있음 — 이름/버전히스토리 등으로 서드파티인지 먼저 확인하고, 애매하면 사용자에게 물어볼 것.
참고2: 21~25번(바탕화면 Astar/St5/우선순위HeapSort/DressUpAsset/JSON) 처리 방식은 위 "Unity 대상 목록 확장" 절 참고. JSON 폴더는 PDF를 아직 안 읽었으니 그 순번 도달 시 정독 필요.
참고3: Unity 프로젝트 폴더 안 스크립트만 보지 말고, DLL/플러그인처럼 프로젝트 바깥에 원본 소스가 따로 있는 경우가 있을 수 있음(UnityDll 사례) — 의심되면 `C:\Study` 전체에서 관련 이름으로 검색해볼 것.
