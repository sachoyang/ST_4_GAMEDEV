# 포트폴리오/면접 정리 작업 계획 (세션 간 이어가기용)

이 파일은 여러 세션에 걸쳐 이 정리 작업을 계속하기 위한 진행 상황 기록이다.
새 세션에서 이 작업을 이어갈 때는 이 파일부터 읽을 것.

## 배경 / 목표
- 국비지원 게임클라이언트(Unity) 과정 수료. 실습 코드를 포트폴리오 + 기술면접 준비용으로 재정리.
- 모든 구현을 나열하지 않고 "핵심 개념 + 그 개념을 가장 잘 보여주는 대표 예제"로 압축.
- 결과물은 이 폴더(`C:\Study\ST_4_GAMEDEV`)에 언어/기술별 md 파일로 저장.

## 중요: 소스 코드 위치
`ST_4_GAMEDEV` 폴더 자체는 원래 비어있었음(정리 결과물 저장용). **실제 실습 코드는 아래 폴더들에 있음**:
- `C:\Study\C` — C/C++ 실습
- `C:\Study\MFC` — MFC 실습
- `C:\Study\D3D` — WinAPI/Direct3D 실습 (`D3D_ST`, `BOARD/Engine` 등)
- `C:\Study\Unity` — Unity 프로젝트 다수

**인코딩 주의**: 원본 `.c`/`.cpp`/`.h` 파일 다수가 CP949(한글 완성형)로 저장되어 있어 UTF-8 도구로 그냥 읽으면 주석이 깨진다. 읽기 전에 `iconv -f CP949 -t UTF-8`로 변환해서 볼 것. 주석에 실습 의도/의사코드가 자세히 적혀있는 경우가 많으므로 반드시 변환해서 확인해야 함 (사용자가 직접 강조한 사항).

## 정리 포맷 (모든 언어 공통, 개념 단위로 반복)
```
### [개념 이름]
- **한 줄 정의**
- **왜 중요한가**
- **내 코드에서 어떻게 썼는지**: 실제 경로 + 최소 스니펫 (전체 파일 X)
- **주의할 점 / 자주 나오는 꼬리 질문**
- **최신 동향 (웹서칭 결과)**: 지금도 유효한지, 대체 기술 있는지. 확인 안 되면 "확인 필요"라고 표시
```
- 각 파일 최상단에 개념 목차(TOC)를 먼저 넣는다.
- 코드가 실제로 어떤 언어인지(파일명과 무관하게) 확인 후 분류한다 — 예: `C:\Study\C\Wordle_c`는 이름과 달리 실제로는 C++(class, enum class, iostream) 코드였음.

## 진행 순서 (사용자가 지정한 순서, 한 번에 하나씩)
1. **C언어** → `c_핵심정리.md` — ✅ 완료 (2026-07-27)
2. **C++** → `cpp_핵심정리.md` — ✅ 완료 (2026-07-27, Part1 기초 + Part2 중급/고급(API/CPP_PLUS) 포함)
3. **WinAPI + MFC** → `winapi_핵심정리.md` / `mfc_핵심정리.md` (파일은 2개, 작업은 한 번에 진행) — ✅ 완료 (2026-07-27)
4. **Unity** → `unity_핵심정리.md` — ⏳ 진행 중 (프로젝트별로 하나씩, 매번 확인받고 진행 — 상세 진행상황은 unity_핵심정리.md 상단 표 참고)
5. **5개 파일 관통 종합 정리** (별도 파일, 예: C 메모리 개념 → C++ 객체 → Unity GC 흐름) — ⏳ 대기 중, **사용자가 다른 모델로 진행할 예정** (이 세션에서 자동으로 진행하지 말 것)

## 3단계(C++) 추가 확정 사항 (2026-07-27)
- `C:\Study\API\CPP_PLUS`(싱글톤, 스택/이중연결리스트 템플릿화, TCP·UDP 소켓, C++11 동시성 심화)는 "같은 시기에 배운 C++ 중급/고급 과정"이라는 사용자 확인에 따라 WinAPI가 아니라 `cpp_핵심정리.md`의 Part 2로 포함시킴. `API/CPP_PLUS`의 `FMODENGIN` 헤더는 실제 사용처가 없어 제외.

## 4단계(WinAPI+MFC) 스코프 확정 사항 (2026-07-27)
- 소스 폴더: `C:\Study\API`(단, `CPP_PLUS` 제외) + `C:\Study\MFC`.
- **`C:\Study\D3D\BOARD\Engine`은 사용자가 개인적으로 별도 작업한 프로젝트라서 이번 정리에서 완전히 제외.** `D3D\D3D_ST`도 이번 WinAPI 단계에서는 다루지 않음(별도 그래픽스/D3D 정리가 필요하면 나중에 논의).
- **`C:\Study\lookis` 폴더는 다른 시설(교육기관)에서 배운 내용이라 전체 제외.**
- 파일 구조: `winapi_핵심정리.md`와 `mfc_핵심정리.md` 2개로 분리하되, 겹치는 개념(메시지 루프, 콜백, 이벤트 기반 구조 등)이 많아 한 번의 조사/작업 세션에서 같이 진행.
- 결과: `winapi_핵심정리.md`(13개 개념, 종합예제 TETORIS) + `mfc_핵심정리.md`(7개 개념, 종합예제 Lotto_peace) 완성. 두 파일이 서로의 항목을 번호로 상호 참조하도록 작성함(예: MFC 2번 메시지맵 ↔ WinAPI 3번 WndProc).

각 단계는: (a) 해당 언어 폴더 구조/개념 목차 초안 제시 → (b) 사용자 컨펌 → (c) 실제 md 작성, 순서로 진행한다. 한 번에 여러 언어를 동시에 진행하지 않는다.

## 각 단계별로 확정된 결정 사항 (시간순)

**1단계(C언어)**
- `C:\Study\C` 안에서 실제로 순수 C인 것: `C_ST/ST.cpp`, `File_ST/miro.cpp` → `c_핵심정리.md`에 포함.
- 실제로는 C++인 것(이름과 무관): `CPP_ST/`, `OOP/`, `TRPG_peace/`, `Wordle_c/` → `cpp_핵심정리.md`로 이월.
- `Critical_Section/`(스레드/임계구역, WinAPI `CreateThread`/`CRITICAL_SECTION` + 일부 C++11 `std::thread` 혼재) → C언어 파일에 개념 대표로 포함(사용자 확인).
- `File_ST/miro.cpp`(미로찾기, 스택을 배열로 직접구현 + 백트래킹)이 C 폴더 전체 대표 예제.

**2단계(C++)**
- `CPP_ST/`, `OOP/`, `Wordle_c/`(이름과 무관하게 실제 C++) → `cpp_핵심정리.md`에 포함.
- `TRPG_peace/`는 클래스 선언만 있고 메서드 본문이 전부 비어있는 미완성 스텁이라 정리에서 제외.
- `OOP/` 프로젝트(Account 상속 계층 + Container<T> 템플릿 + AccManager)를 C++ Part1 대표 예제로 선정.
- 코드 리뷰 중 실제 이슈 발견 → cpp_핵심정리.md 하단 "고쳐볼 만한 부분"에 기록: Account에 가상 소멸자 없음(Container가 기반포인터로 delete), mystring::operator=에 자기대입 가드 없음, override 키워드 미사용.
- `C:\Study\API\CPP_PLUS`(싱글톤, 스택/이중연결리스트 템플릿화, TCP·UDP 소켓, C++11 동시성 심화)를 "같은 시기에 배운 C++ 중급/고급 과정"으로 확인받아 `cpp_핵심정리.md` Part 2로 포함. `FMODENGIN` 헤더는 미사용이라 제외.

**3단계(WinAPI+MFC)**
- 소스: `C:\Study\API`(`CPP_PLUS` 제외) + `C:\Study\MFC`.
- `C:\Study\D3D\BOARD\Engine`은 사용자 개인 별도 프로젝트라 완전 제외. `D3D\D3D_ST`도 이번 단계에서는 다루지 않음(그래픽스/D3D 정리가 필요하면 나중에 별도 논의).
- `C:\Study\lookis` 폴더는 다른 교육기관에서 배운 내용이라 전체 제외.
- 결과: `winapi_핵심정리.md`(13개 개념, 종합예제 TETORIS) + `mfc_핵심정리.md`(7개 개념, 종합예제 Lotto_peace). 두 파일이 서로의 항목을 번호로 상호 참조(예: MFC 2번 메시지맵 ↔ WinAPI 3번 WndProc).

## 4단계(Unity) 진행 방식 (확정, 2026-07-27)
- **다른 언어와 달리 프로젝트 단위로 하나씩, 매번 보고 후 사용자 허락을 받고 다음 프로젝트로 진행.** 한 번에 여러 프로젝트를 정리하지 말 것.
- 순서는 프로젝트 폴더의 **생성일**(Windows `CreationTime`, `Get-Item ... | Select CreationTime`으로 확인) 순.
- 제외 폴더(사용자 확정): `ChainRiposte`, `DGC`, `PUZZLE`, `SoulsServer`, `assets`, `SuperMarioBros` — 전부 개인적으로 별도 진행한 프로젝트.
- 전체 순서와 진행 상태는 `unity_핵심정리.md` 최상단의 표에 기록되어 있음 — 새 세션에서는 그 표를 보고 다음 순번 프로젝트부터 이어가면 됨.
- Unity 프로젝트 안에는 C/C++/C# 문법 학습 내용도 섞여있을 수 있다고 사용자가 언급함 — 발견하면 unity_핵심정리.md에 넣을지 cpp_핵심정리.md/c_핵심정리.md를 보강할지 사용자에게 확인.
- 면접 준비 목적이므로 애매한 설명 없이 "명확하게" 정리할 것 — 사용자가 재차 강조함.
- WinAPI/MFC 정리에서 미리 걸어둔 연결고리: 델타타임 기반 게임 루프(winapi 7번 ↔ Unity Update/FixedUpdate), UI/로직 책임 분리(winapi 10번 ↔ MonoBehaviour vs 순수 C# 클래스), 종료 시 훅(mfc 6번 ↔ OnApplicationQuit) — 관련 프로젝트 정리할 때 실제로 이어줄 것.

## Unity 대상 목록 확장 (2026-07-27, 사용자 추가 요청)
사용자가 바탕화면(`C:\Users\user\Desktop`)에도 정리 대상 Unity 프로젝트가 있다고 알려줘서 확인함. 생성 시점이 불확실해 `unity_핵심정리.md` 목록 맨 뒤(21~25번)에 배치:
- **`Astar`**(A* 길찾기, 9개 스크립트) — 정상적인 학생 프로젝트.
- **`St5`**(3D 액션 게임, `PlayerCtrl.cs` 881줄/`EnemyCtrl.cs` 745줄 등 총 2829줄) — 지금까지 중 가장 큰 프로젝트. 여러 세션에 걸쳐 나눠 다룰 가능성 높음. St1~ST_2 계열과 겹치는 개념(카메라 추적, 체력 등)이 있을 수 있으니 새로운 내용 위주로 압축.
- **`우선순위HeapSort`**(힙 정렬/우선순위 큐, 2개 스크립트 295줄) — 정상적인 학생 프로젝트.
- **`FSM`(바탕화면)**: `Study\Unity\FSM`(16번)과 diff 확인 결과 사실상 동일 프로젝트 — Study 쪽이 인코딩 정상 + `OnStateExit` 구현이 추가된 더 완성된 버전. **바탕화면 FSM은 별도 처리하지 않고 16번에서 한 번만 다룸.**
- **`DressUpAsset`**: 학생 코드 없음, `dressup+assets.unitypackage`(서드파티 캐릭터 에셋) + `Ch2.DressUp.pdf`(ibatstudio 강의자료로 확인, 리스트/아바타 옷갈아입히기/Animator Humanoid 리깅/애니메이션 트랜지션/PlayerPrefs+씬전환 저장 다루는 튜토리얼). 사용자 확인: "PDF 개념을 요약해서 포함"으로 처리 — `unity_핵심정리.md`에서 "내 코드" 대신 "학습한 개념(출처: 강의자료)"로 명시.
- **`JSON`**: 마찬가지로 학생 코드 없음, PDF(`Ch6.JSON사용하기`, `서버프로그래밍01~03`) + 예제 zip(`JSONEx.zip` — 내부 타임스탬프 2017년, 강사 배포 예제로 추정) + `exam_tender.sql`(21MB DB 덤프, 무관한 샘플 데이터로 보임). 이것도 "학습한 개념" 방식으로 요약 예정 — 아직 PDF 상세 내용은 안 읽음, 해당 순번 도달 시 정독 필요.

## 5번 `3dTO2d` 제외 처리 (2026-07-27)
확인 결과 스크립트 7개(726줄) 전부가 Unity 공식 "Starter Assets - FirstPerson Character Controller" 패키지 그대로였고(`FirstPersonController.cs` 등), `IronSpear Content`라는 서드파티 환경 에셋도 포함 — 학생이 직접 짠 코드가 전혀 없어 정리 대상에서 제외. unity_핵심정리.md 표에 🚫 표시로 남겨둠.

## 6번 St3 — 규모가 커서 하위 분할 진행 중 (2026-07-27~)
St3(60개 스크립트, 6676줄)는 `Study1`~`Study7` 폴더별로 5단계(St3-A~E)로 나눠 진행하기로 확정. 세부 진행 상황은 `unity_핵심정리.md`의 "St3 하위 진행 표"에 기록.
- **St3-A**(`Study1`+`Study2`: 델리게이트/이벤트/람다/프로퍼티/싱글톤 개론) — ✅ 완료 (2026-07-27). 특히 `MonoBehaviour`를 `new`로 만들면 안 되는 이유를 직접 실험한 코드(`Property.cs`)가 좋은 발견이었음.
- **St3-B**(`Study3`+`Study4`: 자료구조/제네릭 싱글톤 진화/UI 이벤트 후킹) — ✅ 완료 (2026-07-27). `Study4/Singleton`의 `MonoSingleton1→2→3` 진화(자가참조 제네릭 `where T : MonoSingleton3<T>`, CRTP류 패턴)를 5-7/5-8번(St3-A)과 연결해서 정리함.
- **St3-C**(`Study5`: 에디터 확장 + AssetBundle) — ✅ 완료 (2026-07-27). `[MenuItem]`/`[CustomEditor]` 에디터 확장 + AssetBundle 빌드/로드, 그리고 `WWW`→`UnityWebRequest` API 마이그레이션 실사례(ST_2의 `Application.loadedLevel` 건에 이은 2번째 사례)를 다룸.
- **St3-D**(`Study6`: 인벤토리 시스템 + 커스텀 `PropertyAttribute`/`PropertyDrawer`(`ShowIfAttribute`) + 에디터 툴) — ✅ 완료 (2026-07-27). `ScriptableObject` 상속계층, 리플렉션 기반 `PropertyDrawer`, `EditorWindow` 심화, 세 번째 Deprecated API 사례(`[PreferenceItem]`→`[SettingsProvider]`)까지 다룸. `ItemManager`가 런타임 MonoBehaviour에서 에디터 전용 `AssetDatabase`를 잘못 쓰는 실제 버그도 발견.
- **St3-E**(`Study7`: 프로시저럴 메시 생성 + 커스텀 기즈모) — ✅ 완료 (2026-07-27). `OnDrawGizmos`, `Mesh` 직접 구성(vertices/uv/normals/triangles), 기즈모+메시 결합한 실시간 편집 종합예제, `[ContextMenu]`(4번째 에디터 확장 어트리뷰트), 메시를 에셋으로 저장하는 워크플로우까지 다룸.
- **St3 전체 완료 (2026-07-27).** `unity_핵심정리.md`에 Study1~7을 관통하는 "St3 종합" 요약 섹션도 추가함(MonoBehaviour new 제약의 파급효과, 문자열 기반 API의 반복된 위험성, 3번의 Deprecated API 마이그레이션, 에디터 확장 어트리뷰트 4종 세트 등).

## 7번 3D_ST1 완료 (2026-07-27)
스크립트 66개 중 대부분(`Ch2/Standard Assets`)이 Unity 공식 Standard Assets 패키지 + 서드파티 VFX/지형 에셋(`FT_MagicEffect_Vol04`, `atsV2`)이라 제외하고, 실제 학생 코드 7개 파일만 정리(`VideoPlayer` 인트로, `CharacterController` 이동, 레거시 `Animation`/`CrossFade`/`PlayQueued`, 래그돌 물리, 3D 충돌 데미지). `Ch3/Study1/AnimationEvevnt.cs`는 ST_2에서 이미 다룬 것과 동일해 각주 처리만 함.

## 다음 세션에서 할 일
`unity_핵심정리.md` 상단 표의 8번 `TempleRun`(2026-04-13)부터 이어서 진행. 프로젝트 안에 3dTO2d/3D_ST1처럼 Unity 공식 에셋/서드파티가 섞여있을 수 있으니 먼저 학생 코드 범위부터 확인할 것. 진행 순서: (a) 해당 프로젝트 스크립트 조사 → (b) 뽑아낸 개념 목록을 사용자에게 제시 → (c) 컨펌받고 unity_핵심정리.md에 새 섹션으로 추가 + 상단 표 상태 갱신.
참고1: `ST_2`에서 "9. Etc" 폴더처럼 프로젝트 안에 서드파티 에셋(UltimateJoystick)과 성격이 다른 도구(에디터 확장 등)가 섞여있는 경우가 또 나올 수 있음 — 이름/버전히스토리 등으로 서드파티인지 먼저 확인하고, 애매하면 사용자에게 물어볼 것.
참고2: 21~25번(바탕화면 Astar/St5/우선순위HeapSort/DressUpAsset/JSON) 처리 방식은 위 "Unity 대상 목록 확장" 절 참고. JSON 폴더는 PDF를 아직 안 읽었으니 그 순번 도달 시 정독 필요.
