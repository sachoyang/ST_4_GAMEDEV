# C언어 핵심정리

> 국비 게임클라이언트(Unity) 과정 실습 코드 기반 복습/면접 자료.
> 원본 코드 위치: `C:\Study\C\C_ST`, `C:\Study\C\File_ST`, `C:\Study\C\Critical_Section`
> (원본은 CP949 인코딩이라 VS 밖에서 열면 주석이 깨질 수 있음 — 필요하면 UTF-8로 재변환해서 볼 것)

## 목차

1. [기본 입출력 & 진법 변환](#1-기본-입출력--진법-변환)
2. [함수와 재귀호출](#2-함수와-재귀호출)
3. [반복문 기반 완전탐색](#3-반복문-기반-완전탐색)
4. [1차원 배열 & 문자열 직접 처리](#4-1차원-배열--문자열-직접-처리)
5. [포인터 기초](#5-포인터-기초)
6. [포인터와 배열의 관계](#6-포인터와-배열의-관계)
7. [함수와 포인터 (call by reference)](#7-함수와-포인터-call-by-reference)
8. [2차원 배열](#8-2차원-배열)
9. [rand()/srand() 난수 활용](#9-randsrand-난수-활용)
10. [구조체](#10-구조체)
11. [매크로 함수 & 토큰 결합 연산자(##)](#11-매크로-함수--토큰-결합-연산자)
12. [스택을 배열로 직접 구현 + 백트래킹 (미로찾기)](#12-스택을-배열로-직접-구현--백트래킹-미로찾기)
13. [스레드 & 임계구역 기초 (개념 대표 포함)](#13-스레드--임계구역-기초-개념-대표-포함)

---

## 1. 기본 입출력 & 진법 변환

- **한 줄 정의**: `printf`/`scanf`의 서식 지정자(`%d`, `%x` 등)로 콘솔 입출력과 진법 변환을 처리하는 C의 가장 기본적인 문법.
- **왜 중요한가**: 모든 실습의 출발점이자, 면접에서 "printf 서식지정자 아는 대로 말해보세요" 류의 워밍업 질문으로 자주 등장.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:9-18`
  ```c
  int n;
  printf("10진수 정수 입력: ");
  scanf("%d", &n);
  printf("16진수로 %x", n);
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `scanf`에 `&`를 빼먹으면? → 잘못된 메모리 주소에 쓰기 시도(정의되지 않은 동작).
  - `scanf`의 반환값(성공적으로 읽은 항목 수)을 왜 검사해야 하는가?
- **최신 동향 (웹서칭 결과)**: MSVC는 보안 경고(C4996)와 함께 `scanf_s`/`printf_s`(C11 Annex K) 사용을 유도하지만, Annex K는 선택 사양이라 GCC/Clang 등 리눅스 계열 표준 라이브러리는 구현하지 않아 이식성이 낮다. 실무에서는 `scanf` 계열 대신 `fgets`로 한 줄을 통째로 읽고 `sscanf`로 파싱하거나, C++에서는 `cin`/`cout` 스트림을 쓰는 경우가 많다. ([DevGex](https://devgex.com/en/article/00034955), [jcbhmr.com](https://jcbhmr.com/2024/10/31/til-annex-k/))

---

## 2. 함수와 재귀호출

- **한 줄 정의**: 함수를 자기 자신 안에서 다시 호출해 반복 로직을 표현하는 기법.
- **왜 중요한가**: 재귀는 트리/그래프 순회, 백트래킹(아래 12번 항목) 등 게임 로직 전반에서 반복 등장하며, 스택 오버플로우·종료조건 설계 능력을 면접에서 자주 확인함.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:191-198` — 2의 거듭제곱을 재귀로 구현
  ```c
  int Exp(int k)
  {
      if (k == 0) { return 1; }
      return 2 * Exp(k - 1);
  }
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - 재귀의 종료조건이 없으면? → 스택 오버플로우.
  - 이 재귀를 반복문(iterative)으로 바꾸면 어떤 이점이 있는가? (스택 프레임 오버헤드 제거)
- **최신 동향**: 개념 자체는 변하지 않음. 다만 컴파일러의 꼬리 재귀 최적화(TCO)는 C/C++ 표준이 보장하지 않으므로, 실무(특히 게임처럼 스택이 얕은 환경)에서는 깊은 재귀 대신 반복문+명시적 스택으로 바꾸는 경우가 많다는 점은 언급할 가치가 있음.

---

## 3. 반복문 기반 완전탐색

- **한 줄 정의**: 가능한 모든 경우의 수를 중첩 반복문으로 순회하며 조건을 만족하는 답을 찾는 방식(브루트포스).
- **왜 중요한가**: 알고리즘 사고의 출발점. "시간복잡도를 어떻게 줄일 것인가"로 이어지는 후속 질문의 밑바탕이 됨.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:88-104` — 거스름돈(크리스피/도넛/콜라) 조합 완전탐색
  ```c
  for (int i = 1; i < m / 500 + 1; i++)
      for (int j = 1; j < m / 700 + 1; j++)
          for (int k = 1; k < m / 400; k++)
              if ((500 * i + 700 * j + 400 * k) == m)
                  printf("크리스피(%d개), 도넛(%d개), 콜라(%d개)\n", i, j, k);
  ```
  소수 판별(`ST.cpp:119-151`)도 같은 패턴(모든 약수 후보를 순회).
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - 이 거스름돈 문제를 O(n³)에서 어떻게 줄일 수 있는가? (2중 루프로 줄이고 나머지는 계산으로 유도, 혹은 DP)
  - 소수 판별을 `sqrt(n)`까지만 검사하도록 최적화하면?
- **최신 동향**: 알고리즘 자체는 시대와 무관. 다만 실제 면접에서는 "브루트포스 → 시간복잡도 분석 → 최적화" 흐름으로 답변을 준비해두는 것이 중요.

---

## 4. 1차원 배열 & 문자열 직접 처리

- **한 줄 정의**: `char[]` 배열을 `\0` 종단 문자 기준으로 직접 순회하며 길이 계산, 뒤집기, 회문(palindrome) 판별 등을 구현.
- **왜 중요한가**: C의 문자열은 표준 타입이 아니라 "관습"이라는 것을 보여주는 대표 주제. `strlen`을 안 쓰고 직접 구현해본 경험은 면접에서 "왜 C 문자열이 위험한지" 설명할 때 좋은 근거가 됨.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:563-574` — 회문 판별
  ```c
  void isPalindrome(char str[], int strlen)
  {
      for (int i = 0; i < (strlen - 1) / 2; i++)
      {
          if (str[i] != str[strlen - 1 - i])
          {
              printf("회문이 아닙니다.");
              return;
          }
      }
      printf("회문입니다.");
  }
  ```
  문자열 길이를 직접 세는 부분은 `ST.cpp:246-263`(널 종단자 만날 때까지 카운트).
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `char str[100]`에 `scanf("%s", str)`을 쓸 때의 위험은? → 버퍼 오버플로우(길이 제한 없음).
  - 회문 판별에서 `strlen-1` 계산이 짝수/홀수 길이에서 왜 안전한가?
- **최신 동향**: C의 배열 기반 문자열 처리 자체는 변함없지만, 실무 코드에서는 `scanf("%s", ...)` 대신 `%99s`처럼 폭 제한을 걸거나 `fgets`를 쓰는 것이 정석. C++ 코드베이스라면 애초에 `std::string`으로 대체하는 것이 표준.

---

## 5. 포인터 기초

- **한 줄 정의**: 변수의 메모리 주소를 저장하는 변수(`int* p = &n`)와 그 역참조(`*p`) 연산.
- **왜 중요한가**: C/C++ 면접에서 가장 자주, 가장 깊게 파고드는 주제. 포인터 연산의 우선순위, `const` 위치에 따른 의미 차이를 정확히 아는지가 실력을 가른다.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:310-322` — 두 포인터가 같은 변수를 가리킬 때의 부작용
  ```c
  int n = 10;
  int* p1 = &n;
  int* p2 = p1;
  printf("%d \n", (*p1)++);  // n: 10 -> 11
  printf("%d \n", (*p2)++);  // n: 11 -> 12 (p1, p2가 같은 곳을 가리키므로)
  ```
  `const` 포인터의 제약은 `ST.cpp:441-455`:
  ```c
  void print(const int* ptr)
  {
      printf("%d \n", *ptr);
      int* p = ptr;   // const 제거는 컴파일 경고/에러
      *p = 20;        // 우회해서 수정 - 위험한 패턴의 예시
  }
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `(*p1)++`과 `*p1++`의 차이는? (연산자 우선순위: `*`보다 `++`가 먼저 붙는 건 아니고, 후위 `++`가 `p1`에 걸림 — 괄호 유무가 핵심)
  - `int* const p` vs `const int* p` vs `const int* const p` 차이 설명.
  - 위 예시처럼 `const`를 캐스팅으로 우회하는 게 왜 나쁜 습관인가?
- **최신 동향**: 문법 자체는 안정적. C23에서 `nullptr` 키워드가 도입되어 `NULL` 매크로 대신 타입-안전한 널 포인터 표현이 가능해진 정도가 최근 변화(확인 필요: 컴파일러별 지원 시점은 프로젝트 툴체인에 따라 다름).

---

## 6. 포인터와 배열의 관계

- **한 줄 정의**: 배열 이름은 첫 원소의 주소로 decay되며, `arr[i]`와 `*(arr+i)`는 동일한 연산이다.
- **왜 중요한가**: "배열과 포인터가 같다/다르다"를 정확히 설명하는 것은 단골 면접 질문(완전히 같지 않음: `sizeof` 차이, 배열은 대입 불가 등).
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:361-377`
  ```c
  int arr[5] = { 1,2,3,4,5 };
  int* p = arr;
  for (int i = 0; i < 5; i++)
  {
      *(p + i) += 2;
  }
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `sizeof(arr)` vs `sizeof(p)`의 결과가 다른 이유는?
  - 배열을 함수 인자로 넘기면 포인터로 decay되는데, 그럼 함수 안에서 배열의 크기를 알 수 있는가? (없음 → 별도로 길이를 넘겨야 함, 실제로 `ST.cpp`의 여러 함수가 `arrlen` 파라미터를 따로 받는 이유)
- **최신 동향**: 개념은 C 표준 초기부터 불변. C++에서는 `std::array`/`std::span`으로 배열 크기 정보를 함께 들고 다닐 수 있다는 점이 실무 차이로 언급하기 좋음.

---

## 7. 함수와 포인터 (call by reference)

- **한 줄 정의**: 포인터를 매개변수로 넘겨 함수 내부에서 호출자의 변수를 직접 변경(call by reference)하는 패턴.
- **왜 중요한가**: "C는 call by value만 있다"는 오해를 바로잡고, 포인터로 어떻게 참조 전달을 흉내내는지 설명하는 능력을 봄.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:420-437` — 세 값을 회전시키는 swap
  ```c
  void swap(int* a, int* b, int* c)
  {
      int temp;
      temp = *a;
      *a = *c;
      *c = *b;
      *b = temp;
  }
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - C++이라면 포인터 대신 참조자(`int&`)를 쓰면 뭐가 달라지는가? (문법적으로 간결, null 불가 보장)
  - 이 함수에서 `a`, `b`, `c`가 같은 주소를 가리키면 어떤 문제가 생기는가?
- **최신 동향**: C에서는 지금도 포인터가 유일한 참조 전달 수단. C++로 넘어가면 참조자·스마트 포인터가 표준적으로 대체하는 부분(cpp_핵심정리.md에서 다룰 예정).

---

## 8. 2차원 배열

- **한 줄 정의**: `arr[row][col]` 형태로 행렬 데이터를 표현하고, 인덱스 연산으로 회전/나선형 채우기 등을 구현.
- **왜 중요한가**: 게임에서 타일맵, 보드게임(오목/틱택토), 그리드 기반 충돌처리의 기본기. 코딩테스트에서도 매우 흔한 유형.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:649-666` — 4x4 배열 90도 회전
  ```c
  void spin(int arr[][4])
  {
      int temp[4][4];
      for (int i = 0; i < 4; i++)
          for (int j = 0; j < 4; j++)
              temp[j][3 - i] = arr[i][j];
      // temp -> arr 복사
  }
  ```
  나선형으로 숫자 채우기는 `ST.cpp:699-752`(방향 전환을 `next % 4`로 관리하는 상태기계 패턴).
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - 왜 회전 결과를 바로 `arr`에 쓰지 않고 `temp`에 먼저 담는가? (원본을 훼손하며 읽으면 이미 회전된 값을 다시 읽는 버그 발생)
  - 나선형 채우기에서 "막히면 방향을 바꾼다"는 로직을 어떻게 상태값(`next`)으로 관리했는가?
- **최신 동향**: 알고리즘 패턴 자체는 불변. 실무 그리드 데이터는 캐시 지역성을 위해 2차원 배열 대신 1차원 배열 + 인덱스 계산(`arr[y * width + x]`)으로 다루는 경우가 많다는 점은 최적화 관련 꼬리 질문에 대비해둘 만함.

---

## 9. rand()/srand() 난수 활용

- **한 줄 정의**: `srand(seed)`로 난수 생성기를 초기화하고 `rand()`로 의사난수를 뽑아 쓰는 C 표준 라이브러리 함수.
- **왜 중요한가**: 게임 로직(주사위, 확률, 랜덤 이벤트)의 기본. "시드를 고정하면 왜 재현 가능한 랜덤이 되는가"는 게임 QA/디버깅에서 실무적으로 중요한 질문.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:769-779` — 주사위 두 개
  ```c
  srand((int)time(NULL));
  printf("주사위 결과: %d, %d\n", rand() % 6 + 1, rand() % 6 + 1);
  ```
  가위바위보(`ST.cpp:781-852`), 야구게임 스트라이크/볼(`ST.cpp:854-901`)도 같은 패턴으로 `rand() % N`을 사용.
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `rand() % 6`이 완벽하게 균등하지 않은 이유는? (모듈로 편향, `RAND_MAX`가 6의 배수가 아니면 일부 값이 더 자주 나옴)
  - 매번 실행할 때마다 같은 난수가 나오지 않게 하려면? (`time(NULL)`로 시드를 매번 다르게)
- **최신 동향 (웹서칭 결과)**: CERT MSC30-C는 `rand()`를 품질·이식성 문제로 지양 권고하며, 대안으로 `arc4random()`(BSD 계열) 또는 PCG 알고리즘을 제시한다. 다만 게임 개발에서는 여전히 **시드 고정으로 재현 가능한 난수**가 필요한 경우가 많아, C++에서는 표준 `<random>` 헤더의 `std::mt19937`(메르센 트위스터)를 많이 쓰는 편이며 순수 C 레벨에서는 지금도 `rand()/srand()`가 학습·프로토타입 단계에서 널리 쓰인다. ([SEI CERT](https://wiki.sei.cmu.edu/confluence/display/c/MSC30-C.+Do+not+use+the+rand()+function+for+generating+pseudorandom+numbers))

---

## 10. 구조체

- **한 줄 정의**: 서로 다른 타입의 데이터를 하나의 이름으로 묶는 사용자 정의 타입(`struct`).
- **왜 중요한가**: C에서 객체지향적 데이터 모델링에 가장 가까운 도구. C++ 클래스(cpp_핵심정리.md 예정)로 넘어가기 전 단계로서 면접에서 "구조체와 클래스의 차이"를 물을 때 기준점이 됨.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:922-947` — 구조체 포인터로 멤버 값 교환
  ```c
  typedef struct simple { int data1; int data2; };

  void swap(struct simple* s)
  {
      int temp = s->data1;
      s->data1 = s->data2;
      s->data2 = temp;
  }
  ```
  구조체를 값으로 반환하는 예시(좌표 덧셈)는 `ST.cpp:950-974`.
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `.`과 `->`의 차이는? (포인터를 통한 멤버 접근인지 여부)
  - 구조체를 함수 인자/반환값으로 값 전달하면 발생하는 비용은? (전체 복사 — 크면 포인터/참조로 넘기는 게 유리)
  - C의 `struct`와 C++의 `class`는 기본적으로 뭐가 다른가? (멤버 기본 접근 제한자: `struct`는 public, `class`는 private)
- **최신 동향**: 구조체 자체는 C의 핵심 문법으로 지금도 그대로 쓰임(특히 POD 데이터, 네트워크 패킷, GPU에 넘기는 정점 데이터 등). 다만 C++/Unity(C#) 실무에서는 로직까지 포함하려면 클래스로, 순수 데이터 뭉치는 여전히 구조체로 쓰는 구분이 일반적.

---

## 11. 매크로 함수 & 토큰 결합 연산자(##)

- **한 줄 정의**: `#define`으로 정의한 매크로가 컴파일 전 전처리 단계에서 텍스트 치환되며, `##`는 두 토큰을 이어붙여 새 식별자를 만드는 연산자.
- **왜 중요한가**: 전처리기의 동작 원리(컴파일 전에 단순 치환)를 이해하는지 확인하는 질문. 매크로의 부작용(다중 평가, 타입 무시)을 아는지도 자주 물어봄.
- **내 코드에서 어떻게 썼는지**: `C/C_ST/C_ST/ST.cpp:976-1002`
  ```c
  #define ADD(x, y) x##y

  void SuperPrint() { printf("SuperPrint! \n"); }

  int main(void)
  {
      ADD(Super, Print());   // 전처리 후 SuperPrint()로 치환되어 호출됨
      return 0;
  }
  ```
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - `#define SQUARE(x) x*x`에 `SQUARE(a+b)`를 넣으면? (`a+b*a+b`로 치환되어 의도와 다른 결과 — 괄호로 감싸야 하는 이유)
  - 매크로와 인라인 함수의 근본적 차이는? (매크로는 타입 검사 없음, 인라인 함수는 실제 함수처럼 타입 체크·디버깅 가능)
- **최신 동향 (웹서칭 결과)**: CERT PRE00-C는 함수처럼 쓰는 매크로보다 `inline`/`static` 함수를 권장한다 — 타입 안전성과 디버깅 편의성 때문. 다만 `##` 토큰 결합은 매크로에서만 가능한 고유 기능이라(예: 이름을 조합해 변수/함수를 생성하는 X-Macro 패턴), 코드 생성이 필요한 상황에서는 지금도 쓰인다. ([SEI CERT PRE00-C](https://wiki.sei.cmu.edu/confluence/display/c/PRE00-C.+Prefer+inline+or+static+functions+to+function-like+macros))

---

## 12. 스택을 배열로 직접 구현 + 백트래킹 (미로찾기)

- **한 줄 정의**: 표준 라이브러리 없이 배열과 `top` 인덱스만으로 스택(Push/Pop)을 구현하고, 이를 이용해 미로를 DFS 백트래킹으로 탐색.
- **왜 중요한가**: 이 폴더 전체에서 **C 개념이 가장 밀도 높게 응집된 대표 예제**. 구조체·배열·전역 상태·자료구조 직접 구현·알고리즘(백트래킹)이 하나의 프로그램에 다 들어있어, 면접에서 "자료구조를 직접 구현해본 경험"을 설명할 때 가장 좋은 카드.
- **내 코드에서 어떻게 썼는지**: `C/File_ST/File_ST/miro.cpp`
  - 자취(좌표+방향)를 담는 구조체와 스택 (`miro.cpp:51-90`)
    ```c
    typedef struct Position { int x; int y; int d; } Position;
    Position Trace[(MAZESIZE_X - 2) * (MAZESIZE_Y - 2)]; // 배열로 만든 스택
    int top;
    ```
  - Push/Pop (`miro.cpp:199-217`)
    ```c
    void Push(Position P) { Trace[top] = P; top++; }
    void Pop(Position* P)  { top--; *P = Trace[top]; }
    ```
  - 백트래킹 핵심 로직 (`miro.cpp:117-158`): 4방향 중 갈 수 있는 곳이면 전진하며 현재 위치를 스택에 쌓고(Push), 막히면 스택에서 이전 위치를 꺼내(Pop) 되돌아가면서 남은 방향을 마저 탐색.
  - 파일 최상단(`miro.cpp:7-32`)에 알고리즘 전체를 한글 슈도코드로 미리 정리해둔 주석이 있어, 설계 의도를 그대로 복기할 수 있음.
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - 재귀 DFS로 짰다면 이 코드와 뭐가 달라지는가? (재귀는 콜스택을 자동으로 씀 — 이 코드는 그걸 배열로 직접 관리)
  - `Mark` 배열(방문 체크)이 없으면 어떤 문제가 생기는가? (같은 칸을 무한히 왕복)
  - 스택 배열의 크기(`(MAZESIZE_X-2)*(MAZESIZE_Y-2)`)를 미리 이 값으로 잡은 이유는? (모든 칸을 최대 한 번씩 방문한다고 가정한 상한)
- **최신 동향 (웹서칭 결과)**: 백트래킹/DFS 알고리즘 자체는 여전히 표준 기법(게임 pathfinding에서는 실무적으로 A* 등으로 발전). 다만 코드 안의 `system("cls")`는 CERT에서 지적하는 대표적인 위험 패턴 — 실행 경로에 있는 임의의 `cls.exe`를 사용자가 바꿔치기하면 임의 코드가 실행될 수 있는 명령 주입(command injection) 취약점이다. 콘솔 지우기는 최신 코드에서 Windows Console API(`SetConsoleCursorPosition` + `FillConsoleOutputCharacter`)나 VT100 이스케이프 시퀀스(`\x1b[2J`, Windows 10 이상 지원)로 대체하는 것이 안전하다.

---

## 13. 스레드 & 임계구역 기초 (개념 대표 포함)

> 이 항목은 `Critical_Section/` 폴더(WinAPI `CreateThread`/`CRITICAL_SECTION` + 일부 C++11 `std::thread` 혼재)를 **동시성 개념의 C 레벨 대표 사례**로 포함한 것. WinAPI 함수 자체의 시그니처/사용법은 winapi_핵심정리.md에서 더 다룰 예정.

- **한 줄 정의**: 여러 스레드가 하나의 공유 자원에 동시에 접근할 때 발생하는 경쟁 상태(race condition)를, 임계구역(critical section)으로 보호해 한 번에 하나의 스레드만 접근하게 만드는 동기화 기법.
- **왜 중요한가**: 게임 클라이언트도 렌더링/입력/네트워크 스레드가 분리되는 경우가 많아, 동시성 개념은 실무 면접에서 빠지지 않는 주제. "뮤텍스와 세마포어의 차이"는 특히 자주 나오는 단골 질문.
- **내 코드에서 어떻게 썼는지**: `C/Critical_Section/Thread/Thread/Thread/ThreadStudy1.cpp:1-41` — 개념을 스스로 정리해둔 주석
  ```
  스레드: 프로그램 내에 있는 흐름의 단위
  동기화: 공유자원의 동시접근을 막기위한 문법
          크리티컬섹션, 이벤트, 뮤텍스, 세마포어

  뮤텍스(Mutex): 공유된 자원의 데이터를 여러 "쓰레드"가 접근하는 것을 막는 것
  세마포어(Semaphore): 공유된 자원의 데이터를 여러 "프로세스"가 접근하는 것을 막는 것
  ```
  같은 파일의 실습 코드(`ThreadStudy1.cpp:43-73`, 주석 처리됨)는 `<thread>`로 두 함수를 동시에 실행시켜 동기화 없이 출력이 뒤섞이는 것을 직접 확인하는 예제.
  WinAPI 레벨의 임계구역 보호는 `Critical_Section/Critical section/문제(답안작성용)/Problem01_vs2013/문제/문제.cpp`에서 `CreateThread`/`CRITICAL_SECTION`으로 구현.
- **주의할 점 / 자주 나오는 꼬리 질문**:
  - 뮤텍스 vs 세마포어: 뮤텍스는 "소유권"이 있는 락(잠근 스레드만 풀 수 있음), 세마포어는 카운트 기반으로 여러 스레드/프로세스의 동시 접근 허용 개수를 조절.
  - 임계구역을 안 걸면 이 코드에서 정확히 어떤 결과가 나오는가? (직접 실행해 출력이 섞이는 걸 재현해봤는지가 이해도를 보여줌)
  - 데드락이 발생하는 조건은?
- **최신 동향 (웹서칭 결과)**: `std::thread`는 내부적으로 Windows에서 `_beginthreadex`(→`CreateThread`)를 거치도록 구현되어 있어, CRT 자원 관리 문제를 피하려면 원시 `CreateThread`보다 `std::thread`가 더 안전한 선택으로 권장된다. 동기화 프리미티브도 이식성을 위해 `std::mutex`가 표준 선택지가 되었지만, `CRITICAL_SECTION`은 커널 모드로 내려가지 않는 경량 스핀락이라 경쟁이 적은 상황에서는 여전히 빠르다는 성능 특성이 있고, 언리얼 엔진처럼 실제 게임 엔진 코드베이스에서는 지금도 `CRITICAL_SECTION` 기반 래퍼(`FCriticalSection`)를 유지하고 있다 — 레거시/성능 최적화 목적의 실무 맥락이 분명히 남아있는 영역. ([Microsoft Q&A](https://learn.microsoft.com/en-us/answers/questions/601995/std-thread-vs-beginthread-endthread), [Unreal Engine Forums](https://forums.unrealengine.com/t/use-std-mutex-instead-of-critical-section-in-fcriticalsection-on-windows-platforms/735232))

---

## 이 폴더에서 뺀 것 (C++로 분류)

아래 프로젝트는 이름과 무관하게 실제로는 클래스/상속/`cin`·`cout` 등 C++ 문법을 쓰고 있어 `cpp_핵심정리.md`에서 다룰 예정:
- `CPP_ST/` (함수 오버로딩, class, new/delete)
- `OOP/` (Account → CreditAcc/DonateAcc 상속 구조)
- `TRPG_peace/` (class 기반 RPG 매니저)
- `Wordle_c/` (이름은 C지만 실제로는 class + enum class + iostream)
